//! Test runner for librga that can run on the target device

fn main() {
    println!("Test runner placeholder - build with cross for target device");
    println!("Run: cross build --release --target aarch64-unknown-linux-gnu");
}
